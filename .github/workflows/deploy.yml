name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "14"

      - name: Install frontend dependencies
        working-directory: frontend/
        run: |
          npm install
          npm run build

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.x"

      - name: Install backend dependencies
        working-directory: backend/
        run: |
          pip install -r requirements.txt
          pip install djongo
          pip install pymongo
          pip install mongoengine

      - name: Set up MongoDB connection
        run: |
          python -c "import pymongo; client = pymongo.MongoClient('mongodb+srv://mengistuabebe06:apTd7lwCU3JRrQqb@cluster0.nn8ns0i.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0')"

      - name: Run Django migrations
        working-directory: backend/
        run: |
          python manage.py makemigrations
          python manage.py migrate

      - name: Collect static files for Django
        working-directory: backend/
        run: |
          python manage.py collectstatic --noinput

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          deploy_key: django-insecure-rkz9*=e&1av3d=(xmy*+apv%5@6vhl#*9hy%%gm)hjged2&9e9
          publish_dir: ./dist # Change this to your React build directory
          cname: menge.com # Optional: add a custom domain

      - name: Clean up
        run: |
          # Clean up any temporary files or configurations
